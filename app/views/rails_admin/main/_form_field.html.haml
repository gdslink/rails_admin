- autocompleteAtt ={}
- if(field.type == :password) then  autocompleteAtt = {autocomplete: 'new-password'} end
= form.send field.view_helper, field.method_name, field.html_attributes.reverse_merge({ value: field.form_value, class: 'form-control', required: field.required}).merge(autocompleteAtt)
